{
  "name": "minisnort-ips",
  "version": "1.0.0",
  "description": "MiniSnort IPS Lab with Dashboard",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
